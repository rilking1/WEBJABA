@model WEBJABA.Models.Inventory

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Inventory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Skinid" class="control-label"></label>
                <select asp-for="Skinid" class ="form-control" asp-items="ViewBag.Skinid"></select>
            </div>
            <div class="form-group">
                <label asp-for="PhotoId" class="control-label"></label>
                <select asp-for="PhotoId" class ="form-control" asp-items="ViewBag.PhotoId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

